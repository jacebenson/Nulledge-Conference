meta {
  name: Create Report for an Event
  type: http
  seq: 4
}

post {
  url: {{RingCentralEventsProdURL}}/v1/events/{{eventId}}/reports/{{reportType}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearer_token}}
}

settings {
  encodeUrl: true
}
