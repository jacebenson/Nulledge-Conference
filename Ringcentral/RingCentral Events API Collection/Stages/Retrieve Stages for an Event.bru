meta {
  name: Retrieve Stages for an Event
  type: http
  seq: 1
}

get {
  url: {{RingCentralEventsProdURL}}/v1/events/{{eventId}}/stages
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearer_token}}
}

settings {
  encodeUrl: true
}
