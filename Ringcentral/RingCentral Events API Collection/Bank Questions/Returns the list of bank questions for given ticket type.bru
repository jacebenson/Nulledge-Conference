meta {
  name: Returns the list of bank questions for given ticket type
  type: http
  seq: 1
}

get {
  url: {{RingCentralEventsProdURL}}/v1/tickets/{{ticketId}}/bankQuestions
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearer_token}}
}

settings {
  encodeUrl: true
}
