meta {
  name: Retrieve Event Details
  type: http
  seq: 2
}

get {
  url: {{RingCentralEventsProdURL}}/v1/events/{{eventId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearer_token}}
}

settings {
  encodeUrl: true
}
