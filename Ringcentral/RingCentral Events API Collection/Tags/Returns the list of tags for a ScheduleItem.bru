meta {
  name: Returns the list of tags for a ScheduleItem
  type: http
  seq: 2
}

get {
  url: {{RingCentralEventsProdURL}}/v1/booths/{{scheduleId}}/tags
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearer_token}}
}

settings {
  encodeUrl: true
}
