meta {
  name: Returns the list of tags for a Booth
  type: http
  seq: 1
}

get {
  url: {{ProdURL}}/v1/booths/{{boothId}}/tags
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearer_token}}
}

settings {
  encodeUrl: true
}
