meta {
  name: Retrieve the list of sessions for an Event
  type: http
  seq: 1
}

get {
  url: {{RingCentralEventsProdURL}}/v1/events/{{eventId}}/sessions
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearer_token}}
}

settings {
  encodeUrl: true
}
