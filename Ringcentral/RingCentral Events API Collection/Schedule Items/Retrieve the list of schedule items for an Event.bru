meta {
  name: Retrieve the list of schedule items for an Event
  type: http
  seq: 1
}

get {
  url: {{RingCentralEventsProdURL}}/v1/events/{{eventId}}/scheduleItems
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearer_token}}
}

settings {
  encodeUrl: true
}
